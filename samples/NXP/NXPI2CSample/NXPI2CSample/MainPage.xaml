<Page
    x:Class="NXPI2CSample.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NXPI2CSample"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
            <TextBlock x:Name="ClearTextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,50,0,0"/>
            <TextBlock x:Name="RedTextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,100,0,0"/>
            <TextBlock x:Name="GreenTextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,150,0,0"/>
            <TextBlock x:Name="BlueTextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,200,0,0"/>
        </Grid>

    </Grid>
</Page>
